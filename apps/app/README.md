# @repo/app — React Application

This package contains the React 19 single‑page app (TanStack Router, Tailwind v4, shadcn/ui, Jotai).

For setup, development, testing, and deployment, follow the root README: ../../README.md

## Local Notes

- Dev: `bun --filter @repo/app dev`
- Build: `bun --filter @repo/app build`
- Preview: `bun --filter @repo/app preview`
- Tests: `bun --filter @repo/app test`

Project‑wide environment variables and conventions are documented in the root README. Keep app‑specific details (routes, components, styling) confined to this package.
